angular.module('curryBhariApp')
    .controller("paymentController", ['$scope', '$http', '$q', 'UserService','$state', function(
        $scope,
        $http, $q, UserService, $state) {
        
    }]);
